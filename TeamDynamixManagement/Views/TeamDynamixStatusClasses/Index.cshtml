@model IEnumerable<ApplicationServicesConfigurationManagementDatabaseAccess.TeamDynamixStatusClass>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TicketStatusID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TicketStatusName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TicketStatusDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TicketStatusID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TicketStatusName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TicketStatusDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TeamDynamixStatusClass_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.TeamDynamixStatusClass_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TeamDynamixStatusClass_Id })
        </td>
    </tr>
}

</table>
